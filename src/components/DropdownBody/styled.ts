import styled from 'styled-components';

const StyledDropdownBody = styled.div`
    .FT-dropdown__footer {
        border-top: 1px solid ${(p) => p.theme.palette.gray100};
        padding: 20px 4px 0 4px;
    }
`;

export default StyledDropdownBody;
